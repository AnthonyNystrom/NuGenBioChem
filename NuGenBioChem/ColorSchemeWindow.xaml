<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:NuGenBioChem_Controls="clr-namespace:NuGenBioChem.Controls" 
    xmlns:Fluent="clr-namespace:Fluent;assembly=Fluent" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d" x:Class="NuGenBioChem.ColorSchemeWindow" ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner" SizeToContent="WidthAndHeight" 
    ShowInTaskbar="False" WindowStyle="ToolWindow"
    Title="Color Scheme">
  <Grid>

    <NuGenBioChem_Controls:PeriodicalTable x:Name="periodicalTable" MaterialChanged="OnMaterialChanged"/>

<!--
    <Fluent:Button HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,100,7" Height="25" Width="85" Style="{DynamicResource ButtonBackstageStyle}" Text="Cancel" Size="Small" Click="OnCancelClick"/>
    <Fluent:Button x:Name="okButton" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,190,7" Height="25" Width="85" Style="{DynamicResource ButtonBackstageStyle}" Text="OK" Size="Small" Click="OnOkClick"/>
    <Fluent:Button x:Name="applyButton" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,10,7" Height="25" Width="85" Style="{DynamicResource ButtonBackstageStyle}" Text="Apply" Size="Small" Click="OnApplyClick" IsEnabled="False"/>
-->
  </Grid>
</Window>
